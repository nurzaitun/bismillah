<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title || 'Dashboard' }}</title>

    <link rel="shortcut icon" href="/assets/images/favicon.svg" type="image/x-icon">
    {{ style('assets/css/bootstrap') }}
    {{ style('assets/vendors/perfect-scrollbar/perfect-scrollbar') }}
    {{ style('assets/css/app') }}
    @!section('css')
</head>

<body>
    <nav class="navbar navbar-header navbar-expand navbar-light">
        <a class="d-block d-sm-none" href="#"><span class="navbar-toggler-icon"></span></a>
        <a class="navbar-brand" href="{{route('IndexController.index')}}">NS</a>
      
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
      
          <ul class="navbar-nav d-flex align-items-center navbar-light ms-auto">
              @loggedIn
              <li class="dropdown">
                  <a href="#" data-bs-toggle="dropdown"
                      class="nav-link dropdown-toggle nav-link-lg nav-link-user">
                      <div class="avatar me-1">
                          <img src="/assets/images/avatar/avatar-s-1.png" alt="" srcset="">
                      </div>
                      <div class="d-none d-md-block d-lg-inline-block">Abdul Hakim (SDN 1 WIDODO)</div>
                  </a>
                  <div class="dropdown-menu dropdown-menu-end">
                    <a class="dropdown-item" href="#"><i data-feather="user"></i> Dashboard</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="/auth/logout"><i data-feather="log-out"></i> Logout</a>
                  </div>
              </li>
              @else
              <li class="nav-item">
                <a class="nav-link" href="{{ route('AuthController.login') }}">Masuk</a>
              </li>
              <li class="nav-item">
                <a class="nav-link btn btn-primary text-white" href="{{ route('AuthController.register') }}">Daftar</a>
              </li>
              @endloggedIn
            </ul>
        </div>
    </nav>
    @!section('content')

    {{ script('assets/js/feather-icons/feather.min') }}
    {{ script('assets/js/app') }}
    {{ script('assets/js/main') }}
    

    @!section('js')
</body>

</html>